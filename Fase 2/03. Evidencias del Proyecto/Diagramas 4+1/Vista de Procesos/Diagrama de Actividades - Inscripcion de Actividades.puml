@startuml
title Proceso: Inscripción a Actividades (AUTOMÁTICO)

start
:Vecino selecciona actividad y confirma inscripción;
if (¿Vecino inscrito en la Junta?) then (Sí)
  :Consultar cupos de la actividad;
  if (¿Hay cupo disponible?) then (Sí)
    :Registrar inscripción (estado = Confirmada);
    :Enviar confirmación al vecino\n(Email/WhatsApp);
  else (No)
    if (¿Lista de espera habilitada?) then (Sí)
      :Registrar en lista de espera;
      :Notificar ingreso a lista de espera;
    else (No)
      :Notificar que no hay cupos disponibles;
    endif
  endif
else (No)
  :Notificar que debe inscribirse en la Junta;
endif
stop
@enduml
